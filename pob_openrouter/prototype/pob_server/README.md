# PoB Server (v13 Native)

The native implementation of **The Principle of Being**.

## Quick Start
1. Run `python3 -m venv venv && . ./venv/bin/activate`
2. (option) `cp .env.example .env` (Then Set Your Google API KEY in .env)
3. Run `./run.sh`
